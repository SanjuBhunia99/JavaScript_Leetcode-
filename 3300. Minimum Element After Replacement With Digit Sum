var minElement = function(nums) {
    let ans = Infinity;

    for (let x of nums) {
        let digitSum = 0;
        while (x > 0) {
            digitSum += x % 10;
            x = Math.floor(x / 10);
        }
        ans = Math.min(ans, digitSum);
    }

    return ans;
};
