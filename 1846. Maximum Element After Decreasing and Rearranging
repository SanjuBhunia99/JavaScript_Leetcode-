var maximumElementAfterDecrementingAndRearranging = function(arr) {
    arr.sort((a,b) => a-b);
    const n = arr.length;
    arr[0] = 1;
    for(let i =1; i < n; i++){
        arr[i] = Math.min(arr[i], arr[i - 1] + 1);

    }
    return arr.at(-1);
};
